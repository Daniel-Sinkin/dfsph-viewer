# Local build products
build/
cmake-build-*/
compile_commands.json

# Local-only simulation/HDRI data. Keep available on disk, but out of git.
local/

# Runtime/editor noise
imgui.ini
.DS_Store
.cache/
.venv/
